# Copy to `.env` and fill in keys. Rebuild after changes (`npm run build`).

VITE_RIOT_API_KEY=RGAPI-your-key-here

# OpenAI-compatible Chat Completions API (Phase 9 AI Coach)
# Currently disabled in app (`AI_COACH_ENABLED = false` in src/core/config/app.ts).
# VITE_OPENAI_API_KEY=sk-your-key-here
# Optional:
# VITE_OPENAI_BASE_URL=https://api.openai.com/v1
# VITE_OPENAI_MODEL=gpt-4o-mini
